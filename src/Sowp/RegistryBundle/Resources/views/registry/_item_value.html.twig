{% if value.type == constant('Sowp\\RegistryBundle\\Entity\\Attribute::TYPE_FILE') %}
    {% if value.name %}
        <a href="{{ web_path(value.path) }}">{{ value }}</a>
    {% else %}
        No file
    {% endif %}
{% else %}
    {{ value }}
{% endif %}
{% extends 'base.html.twig' %}

{% block body %}
    <div class="pull-right">
        {{ include('@SowpRegistry/registry/_btn_export_csv.html.twig') }}
    </div>

    <h1>{{ registry }}</h1>

    {% if registry.type == constant('TYPE_TABLE', registry) %}
        {{ include('@SowpRegistry/registry/layout/_table.html.twig') }}
    {% else %}
        {{ include('@SowpRegistry/registry/layout/_grid.html.twig') }}
    {% endif %}

{% endblock body %}